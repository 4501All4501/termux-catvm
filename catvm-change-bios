#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if (($# != 1)); then
  printf 'Usage: %s <bios-name>\n' "$(basename "$0")" >&2
  exit 64
fi

exec "${SCRIPT_DIR}/catvm.sh" bios "$1"
