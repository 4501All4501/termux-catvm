# termux-catvm

## Command verification

- Command behavior is documented in `docs/COMMAND_MATRIX.md`.
- Run `scripts/check-commands.sh validate` to verify every documented command maps to a concrete implementation function and is not marked as a placeholder.
